<view class="wrap card">
  <view class="title">我的地址</view>
  <view wx:for="{{addresses}}" wx:key="name" class="item">
    <view>{{item.name}}</view>
    <view>{{item.detail}}</view>
    <view class="row"><text>默认当前地址</text><text>修改</text></view>
  </view>
</view>
